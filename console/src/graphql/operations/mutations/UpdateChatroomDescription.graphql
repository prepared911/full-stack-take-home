mutation UpdateChatroomDescription(
  $id: ID!
  $description: String!
) {
  updateChatroomDescription(
    input: {
      id: $id
      description: $description
    }
  ) {
    chatroom {
      ...ChatroomData
    }
  }
}