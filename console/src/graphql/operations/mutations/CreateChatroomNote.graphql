mutation CreateChatroomNote(
  $note: String
  $chatroomId: ID!
) {
  createChatroomNote(
    input: {
      note: $note
      chatroomId: $chatroomId
    }
  ) {
    chatroomNote {
      ...ChatroomNoteData
    }
  }
}
