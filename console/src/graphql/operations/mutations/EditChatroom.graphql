mutation EditChatroom(
  $description: String
  $chatroomId: ID!
) {
  editChatroom(
    input: {
      chatroomId: $chatroomId
      description: $description
    },
  ) {
    chatroom {
      ...ChatroomData
    }
  }
}